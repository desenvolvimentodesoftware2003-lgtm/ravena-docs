2. VALIDAR CONCEITO NA BUSCA

def validar_conceito(pergunta, item):
    conceitos_item = item.get("conceitos", [])
    return any(c in pergunta.lower() for c in conceitos_item)

 3. APLICAR FILTRO

if not validar_conceito(pergunta, melhor_item):
    score *= 0.7  # penaliza