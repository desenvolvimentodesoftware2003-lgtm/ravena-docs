# ============================================
# RAVENA
# BLOCO 9 — NORMALIZAÇÃO DE PERGUNTAS
# ============================================

import re

def limpar_texto(texto):

    texto = texto.lower()

    texto = re.sub(r"[^\w\s]", "", texto)

    texto = texto.strip()

    return texto