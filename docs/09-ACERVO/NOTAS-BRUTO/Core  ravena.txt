============ def remover_acentos(texto):     if isinstance(texto, str):         nfkd_form = unicodedata.normalize('NFKD', texto)         return ''.join([c for c in nfkd_form if not unicodedata.combining(c)])     return texto def preprocessar_texto(texto, remover_stopwords=True, lematizar=False, stemizar=False):     if not isinstance(texto, str):         return ""     texto = texto.lower()     texto = remover_acentos(texto)     texto = re.sub(r'[^a-z0-9\s]', '', texto) # Remove caracteres especiais, mantem letras e numeros     tokens = word_tokenize(texto, language='portuguese')     if remover_stopwords:         stop_words = set(stopwords.words('portuguese'))         tokens = [token for token in tokens if token not in stop_words]     if lematizar:         doc = nlp_pt(" ".join(tokens)) # Processa com SpaCy         tokens = [token.lemma_ for doc in doc]     if stemizar:         stemmer = RSLPStemmer()         tokens = [stemmer.stem(token) for token in tokens]     return " ".join(tokens) def extrair_palavras_chave(pergunta):     doc = nlp_pt(pergunta.lower())     palavras_chave = []     for token in doc:         # Exclui stopwords, pontuações, espaços e termos que não são substantivos ou adjetivos         if not token.is_stop and not token.is_punct and not token.is_space and token.pos_ in ['NOUN', 'PROPN', 'ADJ']:             palavras_chave.append(token.text)     return ' '.join(palavras_chave) def calcular_similaridade_fuzzy(texto1, texto2):     return fuzz.ratio(texto1, texto2) / 100.0 def calcular_similaridade_cosseno(embedding1, embedding2):     # Ensure inputs are numpy arrays for cosine_similarity and float64 dtype     if not isinstance(embedding1, np.ndarray):         embedding1 = np.array(embedding1, dtype=np.float64)     if not isinstance(embedding2, np.ndarray):         embedding2 = np.array(embedding2, dtype=np.float64)     # Reshape if 1D array to 2D for sklearn's cosine_similarity     if embedding1.ndim == 1:         embedding1 = embedding1.reshape(1, -1)     if embedding2.ndim == 1:         embedding2 = embedding2.reshape(1, -1)     return cosine_similarity(embedding1, embedding2)[0][0] def entidade_match(pergunta_original, item_texto):     # Processa os textos com SpaCy para extrair entidades nomeadas     pergunta_doc = nlp_pt(pergunta_original.lower())     item_texto_doc = nlp_pt(item_texto.lower())     entidades_pergunta = {ent.text for ent in pergunta_doc.ents if ent.label_ in ['PERSON', 'ORG', 'LOC', 'MISC']}     entidades_item = {ent.text for ent in item_texto_doc.ents if ent.label_ in ['PERSON', 'ORG', 'LOC', 'MISC']}     # Retorna True se houver alguma entidade em comum     return bool(entidades_pergunta.intersection(entidades_item)) # 1. PRÉ-PROCESSAMENTO (do pseudocódigo) def normalizar(texto):     return texto.lower().strip() # 2. DETECÇÃO DE INTENÇÃO (CRÍTICO) (do pseudocódigo) # Assumes INTENCOES is a global variable from BLOCO 5 def detectar_intencao(pergunta):     pergunta_normalizada = normalizar(pergunta)     for tipo, padrões in globals()['INTENCOES'].items(): # Access global INTENCOES         for p in padrões:             if p in pergunta_normalizada:                 return tipo     return "geral" # 5. FILTRO ESTRUTURAL (do pseudocódigo) def mesma_intencao(p1, p2):     return detectar_intencao(p1) == detectar_intencao(p2) def overlap(p1, p2):     s1 = set(normalizar(p1).split())     s2 = set(normalizar(p2).split())     return len(s1 & s2) / max(len(s1), 1) def extrair_termos_relevantes(texto):     # Reutiliza stopwords do NLTK, já carregadas     stop_words = set(stopwords.words('portuguese'))     # Pré-processa o texto para remover acentos e caracteres especiais, e tokeniza     texto_processado = preprocessar_texto(texto, remover_stopwords=False, lematizar=False, stemizar=False) # Não remove stopwords ainda     palavras = texto_processado.split()     return [p for p in palavras if p not in stop_words and len(p) > 3] def remover_duplicados(lista_de_candidatos):     # Usa o ID único de cada item para identificar duplicados     ids_vistos = set()     candidatos_unicos = []     for candidato in lista_de_candidatos:         if candidato['id'] not in ids_vistos:             candidatos_unicos.append(candidato)             ids_vistos.add(candidato['id'])     return candidatos_unicos def conflito_semantico(pergunta1, pergunta2):     # Placeholder para uma função de detecção de conflito semântico mais sofisticada     # Por enquanto, retorna False para não aplicar penalidade     # Em uma implementação real, pode-se usar um LLM para verificar se os significados são contraditórios     return False # Implementação simplificada limpar_memoria() for cat in categorias_dados:     print(cat["nome"])     # Gerar embeddings para a pergunta e a descrição da categoria     pergunta_embedding = gerar_embedding(pergunta)     descricao_embedding = gerar_embedding(cat["descricao"])     # Calcular a similaridade de cosseno entre os embeddings     similaridade = calcular_similaridade_cosseno(pergunta_embedding, descricao_embedding)     print(similaridade) for cat in categorias_dados:     # Gerar embedding da pergunta uma vez por categoria, se 'pergunta' for constante no loop     pergunta_embedding = gerar_embedding(pergunta)     scores = [calcular_similaridade_cosseno(pergunta_embedding, gerar_embedding(ex)) for ex in cat["exemplos"]]     print(max(scores)) import os from cryptography.fernet import Fernet, InvalidToken # Importar InvalidToken # ============================================================ # BLOCO 8 — CRIPTOGRAFIA # ============================================================ # Inicialização da chave de criptografia # Certifique-se de que a variável CHAVE_PATH está definida no BLOCO 5 CRIPTOGRAFIA_ATIVADA = False # Definir como True para ativar a criptografia if CRIPTOGRAFIA_ATIVADA:     def gerar_chave():         chave = Fernet.generate_key()         with open(CHAVE_PATH, 'wb') as file:             file.write(chave)         print(f"Nova chave de criptografia gerada em: {CHAVE_PATH}")         return chave     def carregar_chave():         if not os.path.exists(CHAVE_PATH):             return gerar_chave()         with open(CHAVE_PATH, 'rb') as file:             chave = file.read()         print(f"Chave de criptografia carregada de: {CHAVE_PATH}")         return chave     chave_criptografia = carregar_chave()     fernet = Fernet(chave_criptografia)     def criptografar(texto):         if isinstance(texto, str):             return fernet.encrypt(texto.encode('utf-8')).decode('utf-8')         return texto     def descriptografar(texto_criptografado):         if isinstance(texto_criptografado, str):             try:                 return fernet.decrypt(texto_criptografado.encode('utf-8')).decode('utf-8')             except InvalidToken:                 print("AVISO: Token de criptografia inválido detectado durante a descriptografia. O texto original será retornado.")                 return texto_criptografado # Retorna o texto criptografado se falhar         return texto_criptografado else:     # Funções mock quando a criptografia está desativada     def gerar_chave():         print("Criptografia desativada. Nenhuma chave gerada.")         return None     def carregar_chave():         print("Criptografia desativada. Nenhuma chave carregada.")         return None     chave_criptografia = None     fernet = None     def criptografar(texto):         # Criptografia desativada         return texto     def descriptografar(texto_criptografado):         # Criptografia desativada         return texto_criptografado # --- Funções de Estado, Leitura Segura e Escrita Consistente --- def esta_criptografado(dado):     # Verifica se o dado é uma string e começa com a marca de criptografia (se aplicável)     # Ou, de forma mais robusta, se CRIPTOGRAFIA_ATIVADA é True e o dado é uma string     # que não pode ser decodificada diretamente (ex: contém caracteres não-UTF-8)     return isinstance(dado, str) and dado.startswith("ENC::") def ler_dado(dado):     if esta_criptografado(dado):         try:             return descriptografar(dado.replace("ENC::", "")) # Remove o prefixo antes de descriptografar         except InvalidToken:             print("ERRO: Token de criptografia inválido durante leitura segura. Retornando dado original.")             return dado         except Exception as e:             print(f"ERRO: Falha na descriptografia durante leitura segura: {e}. Retornando dado original.")             return dado     return dado def salvar_dado(dado, usar_cripto):     if usar_cripto and CRIPTOGRAFIA_ATIVADA:         # Adiciona um prefixo para indicar que o dado está criptografado         return "ENC::" + criptografar(dado)     return dado print("BLOCO 8 — CRIPTOGRAFIA carregado e chave inicializada (ativado: " + str(CRIPTOGRAFIA_ATIVADA) + ").") # ============================================================ # BLOCO 24 — FAXINA AUTOMÁTICA (MIGRAÇÃO E RECLASSIFICAÇÃO) # ============================================================ def realizar_faxina_na_memoria():     print("--- INICIANDO FAXINA SEMÂNTICA NA RAVENA ---")     print("DEBUG: Verificando caminho da base de conhecimento...")     # 1. Mapear arquivos antigos na raiz da pasta de conhecimento     if not os.path.exists(CONHECIMENTO_BASE_PATH):         print("DEBUG: Pasta de conhecimento não encontrada ou vazia.")         print("Pasta de conhecimento não encontrada.")         return     print(f"DEBUG: Pasta de conhecimento encontrada: {CONHECIMENTO_BASE_PATH}")     arquivos_na_raiz = [f for f in os.listdir(CONHECIMENTO_BASE_PATH) if f.endswith('.json')]     print(f"DEBUG: Arquivos JSON na raiz da pasta: {arquivos_na_raiz}")     itens_migrados = 0     for arquivo in arquivos_na_raiz:         caminho_antigo = os.path.join(CONHECIMENTO_BASE_PATH, arquivo)         # Ignorar se for um diretório (já organizado)         if os.path.isdir(caminho_antigo):             print(f"DEBUG: Ignorando diretório: {caminho_antigo}")             continue         print(f"Lendo arquivo antigo: {arquivo}...")         memorias_antigas = carregar_conhecimento_bruto(caminho_antigo)         for item in memorias_antigas:             # Descriptografar para entender o que é o assunto             pergunta_original = descriptografar(item['pergunta'])             # RECLASSIFICAÇÃO: O momento "Tony Stark"             # Usamos o novo Bloco 15 para decidir onde isso realmente deve morar             nova_categoria = classificar_tipo_pergunta(pergunta_original)             # Salvar na nova estrutura de pastas (Bloco 12)             memoria_destino = carregar_conhecimento(nova_categoria)             # Verificar se já não existe para não duplicar             if not any(it['id'] == item['id'] for it in memoria_destino):                 item['categoria'] = nova_categoria # Atualiza o metadado                 memoria_destino.append(item)                 salvar_conhecimento(memoria_destino, nova_categoria)                 itens_migrados += 1         # Opcional: Renomear o arquivo antigo para não processar de novo         os.rename(caminho_antigo, caminho_antigo + ".backup")         print(f"DEBUG: Arquivo {arquivo} processado e renomeado para {arquivo}.backup")     print(f"--- FAXINA CONCLUÍDA: {itens_migrados} itens organizados em novas pastas! ---") def carregar_conhecimento_bruto(path):     """Auxiliar para ler arquivos sem a lógica de subpastas do Bloco 12"""     try:         with open(path, 'r', encoding='utf-8') as f:             return json.load(f)     except:         return [] # Para rodar a faxina, basta descomentar a linha abaixo e dar Play: print("DEBUG: Chamando realizar_faxina_na_memoria()...") realizar_faxina_na_memoria() print("DEBUG: Chamada a realizar_faxina_na_memoria() finalizada.") import numpy as np from sklearn.metrics.pairwise import cosine_similarity # ============================================================ # BLOCO 23 — CLUSTERIZAÇÃO AUTOMATICA DAS MEMÓRIA # ============================================================ def gerar_clusters_memoria(n_clusters=3):     global kmeans_model, cluster_labels, MEMORY_EMBEDDINGS, BASE_EMBEDDINGS     print("--- Iniciando clusterização semântica ---")     if not BASE_EMBEDDINGS:         print("WARN_CLUSTER: Nenhuma memória para clusterizar.")         kmeans_model = None         cluster_labels = {}         return     # Extrai os embeddings das perguntas da BASE_EMBEDDINGS     embeddings_list = []     item_ids = []     for item_id, item_data in BASE_EMBEDDINGS.items():         if 'embedding_pergunta' in item_data and isinstance(item_data['embedding_pergunta'], np.ndarray):             embeddings_list.append(item_data['embedding_pergunta'])             item_ids.append(item_id)     if not embeddings_list:         print("WARN_CLUSTER: Nenhuma embedding de pergunta encontrada na BASE_EMBEDDINGS para clusterizar.")         kmeans_model = None         cluster_labels = {}         return     # Converte a lista de embeddings para um array NumPy com dtype float32 para KMeans     X = np.array(embeddings_list, dtype=np.float32)     if X.shape[0] < n_clusters:         print(f"WARN_CLUSTER: Número de itens ({X.shape[0]}) é menor que o número de clusters desejado ({n_clusters}). Não é possível clusterizar.")         kmeans_model = None         cluster_labels = {}         return     try:         kmeans_model = KMeans(n_clusters=n_clusters, random_state=42, n_init=10)         kmeans_model.fit(X)         cluster_labels = {item_ids[i]: kmeans_model.labels_[i] for i in range(len(item_ids))}         print(f"--- Clusterização finalizada: {X.shape[0]} itens clusterizados em {n_clusters} clusters. ---")     except Exception as e:         print(f"ERROR_CLUSTER: Erro durante a clusterização: {e}")         kmeans_model = None         cluster_labels = {} print("BLOCO 23 — CLUSTERIZAÇÃO AUTOMATICA DAS MEMÓRIA carregado.") # Adicionar esta linha para garantir que o diretório base de conhecimento exista os.makedirs(CONHECIMENTO_BASE_PATH, exist_ok=True) gerar_clusters_memoria(n_clusters=3) # Ajuste o número de clusters conforme necessário # ============================================================ # BLOCO 19 — SÍNTESE DE MÚLTIPLAS RESPOSTAS # ============================================================ # Esta função receberia uma lista de respostas potenciais (com seus scores, talvez) # e tentaria combiná-las ou selecionar a mais abrangente/relevante. # Para uma implementação futura, poderia usar técnicas de sumarização ou ranking. def sintetizar_respostas(respostas_candidatas):     if not respostas_candidatas:         return "Não foi possível sintetizar uma resposta." # Mensagem de fallback     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_SINTESE: Sintetizando a partir de {len(respostas_candidatas)} respostas candidatas.")     # Por simplicidade, vamos retornar a resposta do item com maior score.     # Assumimos que 'respostas_candidatas' é uma lista de tuplas (resposta, score)     # e que já está ordenada ou que o primeiro item é o melhor.     # Se não houver score, apenas retorna a primeira.     # Exemplo: Selecionar a resposta do item com maior score (se disponível)     if isinstance(respostas_candidatas[0], tuple) and len(respostas_candidatas[0]) > 1:         # Se os candidatos vierem com score, pega a resposta do que tiver o maior score.         # Caso contrário, apenas pega a primeira resposta.         melhor_resposta = max(respostas_candidatas, key=lambda x: x[1])[0]     else:         melhor_resposta = respostas_candidatas[0]     return melhor_resposta print("BLOCO 19 — SÍNTESE DE MÚLTIPLAS RESPOSTAS carregado.") # ============================================================ # BLOCO 19.1 — FILTRAGEM, RERANK E DECISÃO (NOVO PIPELINE) # ============================================================ def filtrar_candidatos(pergunta_original, candidatos):     filtrados = []     pergunta_normalizada = normalizar(pergunta_original)     for c in candidatos:         item_pergunta_normalizada = normalizar(c["item"]["pergunta"])         score = c["score"]         # BLOQUEIO OU PENALIDADE por intenção diferente         if not mesma_intencao(pergunta_normalizada, item_pergunta_normalizada):             score *= 0.5             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_FILTRO: Penalidade de intenção para '{item_pergunta_normalizada}'. Novo score: {score:.2f}")         # Penalidade por baixo overlap de palavras         if overlap(pergunta_normalizada, item_pergunta_normalizada) < 0.3:             score *= 0.7             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_FILTRO: Penalidade de overlap para '{item_pergunta_normalizada}'. Novo score: {score:.2f}")         # Impede que o score seja negativo ou excessivamente baixo         c["score_filtrado"] = max(0.0, score)         filtrados.append(c)     return filtrados def reranquear_candidatos(pergunta, embedding, candidatos):     resultado = []     termos_pergunta = extrair_termos_relevantes(pergunta)     for item in candidatos:         # Garantir que 'score_base' exista no item         score = item.get('score_base', 0.0)         termos_item = extrair_termos_relevantes(item['pergunta'])         # validação por interseção de termos         intersecao = len(set(termos_pergunta) & set(termos_item))         if intersecao == 0:             score -= 0.15  # penalidade forte             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_RERANK: Penalidade por zero interseção para '{item['pergunta']}'. Novo score: {score:.2f}")         else:             score += intersecao * 0.05             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_RERANK: Bônus por interseção ({intersecao}) para '{item['pergunta']}'. Novo score: {score:.2f}")         # penalidade por conflito semântico leve         if conflito_semantico(pergunta, item['pergunta']):             score -= 0.10             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_RERANK: Penalidade por conflito semântico para '{item['pergunta']}'. Novo score: {score:.2f}")         # 🔹 bônus por cluster (se existir)         if 'kmeans_model' in globals() and kmeans_model is not None and 'cluster_labels' in globals() and item['id'] in cluster_labels:             # Verifica se a pergunta está no mesmo cluster do item. Isso exigiria re-calcular o cluster da pergunta.             # Para simplificar, vamos dar o bônus se o item estiver clusterizado e o sistema de cluster estiver ativo.             score += 0.05             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_RERANK: Bônus por cluster para '{item['pergunta']}'. Novo score: {score:.2f}")         # BOOST DE MATCH EXATO         if normalizar(pergunta) == normalizar(item["pergunta"]):             score += 0.5             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_RERANK: Boost por match exato para '{item['pergunta']}'. Novo score: {score:.2f}")         # Adicionar o bônus de acesso         acessos = item.get("acessos", 0)         score *= (1 + (acessos * PESOS['access_bonus_per_access']))         if MODO_EXPLICACAO == "tecnico":             print(f"DEBUG_RERANK: Boost por acessos ({acessos}) para '{item['pergunta']}'. Novo score: {score:.2f}")         item['score_final'] = score         resultado.append(item)     return sorted(resultado, key=lambda x: x['score_final'], reverse=True) def decidir(candidatos):     # Esta função será integrada na nova processar_pergunta, mas mantida para clareza.     if not candidatos:         return None, 0.0 # Nenhum candidato     melhor = candidatos[0]     if melhor["score_final"] < GLOBAL_MATCH_THRESHOLD:         if MODO_EXPLICACAO == "tecnico":             print(f"DEBUG_DECISAO: Melhor score ({melhor['score_final']:.2f}) abaixo do threshold ({GLOBAL_MATCH_THRESHOLD:.2f}). Fallback.")         return None, melhor["score_final"]  # fallback     if MODO_EXPLICACAO == "tecnico":         print(f"DEBUG_DECISAO: Melhor match encontrado com score final: {melhor['score_final']:.2f}")     return melhor, melhor["score_final"] def pode_aprender(confianca_categoria, score_final, item_id=None):     # Não aprende perguntas vazias ou respostas muito curtas     # if not pergunta_original or not resposta_gerada or len(resposta_gerada) < MIN_COMPRIMENTO_RESPOSTA_AUTO_APRENDER:     #     if MODO_EXPLICACAO == "tecnico":     #         print(f"LOG_AUTO_LEARN: Não auto-aprendendo: pergunta/resposta inválida ou muito curta. Comprimento: {len(resposta_gerada)}")     #     return False     # Bloqueio de aprendizado com baixa confiança     if confianca_categoria < LIMITE_CONFIANCA_MIN_AUTO_APRENDER:         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_AUTO_LEARN: Não auto-aprendendo: baixa confiança na categoria ({confianca_categoria:.2f} < {LIMITE_CONFIANCA_MIN_AUTO_APRENDER:.2f}).")         return False     if score_final < PONTUACAO_MIN_AUTO_APRENDER or score_final > PONTUACAO_MAX_AUTO_APRENDER:         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_AUTO_LEARN: Não auto-aprendendo: score final ({score_final:.2f}) fora do intervalo permitido ([{PONTUACAO_MIN_AUTO_APRENDER:.2f}, {PONTUACAO_MAX_AUTO_APRENDER:.2f}]).")         return False     # Uma lógica mais avançada poderia verificar se o item já existe com alta confiança     # ou se a pergunta é realmente "nova" e não uma variação de algo já sabido.     # Por enquanto, se passou pelos critérios acima, consideramos que pode aprender/atualizar.     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_AUTO_LEARN: Auto-aprendizagem permitida. Confiança Categoria: {confianca_categoria:.2f}, Score Final: {score_final:.2f}")     return True print("BLOCO 19.1 — FILTRAGEM, RERANK E DECISÃO carregado.") # ============================================================ # BLOCO 20 — GERAÇÃO DE RESPOSTAS CONTEXTUALIZADAS (TO BE IMPLEMENTED) # ============================================================ # Este bloco será responsável por usar um modelo de linguagem generativo (LLM) # para criar respostas se nenhum conhecimento direto for encontrado ou se for # necessária uma explicação mais elaborada/resumo. def gerar_resposta_contextualizada(pergunta_original, memoria_curta, top_candidatos=None):     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_GERACAO: Tentativa de geração contextualizada para '{pergunta_original}'.")     # Placeholder para integração futura com modelos como Google Gemini.     # A lógica aqui poderia verificar:     # 1. Se a confiança do melhor match é baixa (top_candidatos[0][1] < threshold).     # 2. Se a pergunta pede por uma explicação que exige sumarização ou inferência.     # 3. Se a pergunta é totalmente nova e não tem match na base de conhecimento.     # Simulação de uma resposta generativa se a confiança for muito baixa para um match direto     if top_candidatos and top_candidatos[0][1] < LOW_CONFIDENCE:         return f"Com base na sua pergunta sobre '{pergunta_original}' e considerando o contexto, posso inferir que... (Funcionalidade de Geração de Resposta via LLM a ser implementada)."     return "" # Retorna vazio se não houver necessidade de geração ou se a geração for falha import numpy as np from sklearn.metrics.pairwise import cosine_similarity # ============================================================ # BLOCO 21 — TRATAMENTO DE EXCEÇÕES E FALLBACK # ============================================================ # Este bloco lida com situações onde o sistema não consegue fornecer uma resposta # satisfatória, seja por erro, falta de conhecimento ou baixa confiança. def lidar_com_excecao(pergunta_usuario, tipo_erro="geral", detalhes="Sem detalhes."):     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_ERRO: Exceção detectada para '{pergunta_usuario}'. Tipo: {tipo_erro}. Detalhes: {detalhes}")     # Em uma implementação real, aqui o sistema poderia:     # - Registrar o erro em um log de monitoramento.     # - Alertar um operador humano.     # - Tentar uma estratégia de busca alternativa (e.g., pesquisa na web).     # Por ora, retorna uma mensagem de erro padrão.     return "Desculpe, ocorreu um erro interno ao processar sua pergunta. Por favor, tente novamente mais tarde." def fallback_resposta(pergunta, candidatos, contexto=None):     # Esta função será substituída por fallback_inteligente, mas mantida por compatibilidade temporária     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_FALLBACK: Ativando fallback para '{pergunta}'.")     diagnostico = fallback_diagnostico(pergunta, candidatos, contexto) if contexto else {}     if diagnostico.get("motivo") == "baixa_similaridade":         mensagem = "Minha confiança nesta informação é um pouco baixa. Tente reformular a pergunta ou me ensine mais sobre isso."     elif diagnostico.get("motivo") == "intencao_incorreta":         mensagem = "Não tenho certeza sobre a intenção da sua pergunta com o conhecimento que possuo. Pode ser mais específico?"     elif diagnostico.get("motivo") == "categoria_insegura":         mensagem = "Não consegui classificar a categoria da sua pergunta com segurança. Tente reformular ou me ensine o contexto."     elif diagnostico.get("motivo") == "erro_dado_memoria":         mensagem = "Parece haver um problema com o dado na minha memória. Vou tentar recarregá-la. Por favor, repita sua pergunta."     else:         mensagem = "Ainda não sei isso com certeza. Você pode me ensinar?"     return {         "mensagem": mensagem,         "acao": "sugestao_aprender",         "pergunta_original": pergunta,         "detalhes_motivo": diagnostico.get("motivo", "desconhecido"),         "debug_context": diagnostico # Retorna o diagnóstico completo     } def validar_resposta(item):     if not item:         return False     if item.get('score_final', 0.0) < GLOBAL_MATCH_THRESHOLD:         return False     return True def fallback_inteligente(pergunta, candidatos):     if not candidatos:         if MODO_EXPLICACAO == "tecnico":             print("LOG_FALLBACK: Nenhum candidato encontrado para fallback.")         return "Não encontrei nenhuma informação relacionada. Pode me ensinar?"     melhor = candidatos[0]     if melhor.get('score_base', 0.0) > LOW_CONFIDENCE:         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_FALLBACK: Encontrado candidato com score_base médio ({melhor.get('score_base', 0.0):.2f}). Sugerindo resposta similar.")         # Para retornar a resposta, precisamos garantir que o 'item' esteja na estrutura correta         # A resposta pode estar em 'item['item']['resposta']' ou 'item['resposta']' dependendo da estrutura dos candidatos         resposta_sugerida = melhor.get('resposta', melhor.get('item', {}).get('resposta', ''))         if resposta_sugerida:             return f"Encontrei algo parecido, mas não tenho certeza:\n→ {resposta_sugerida}"         else:             return "Encontrei algo parecido, mas a resposta está incompleta. Pode me ensinar?"     if MODO_EXPLICACAO == "tecnico":         print("LOG_FALLBACK: Score_base muito baixo. Solicitando mais informações.")     return "Ainda não sei isso com segurança. Pode me ensinar?" print("BLOCO 21 — TRATAMENTO DE EXCEÇÕES E FALLBACK carregado.") # ============================================================ # BLOCO 25 — INTEGRAÇÃO COM FERRAMENTAS EXTERNAS (TO BE IMPLEMENTED) # ============================================================ # Este bloco é um placeholder para futura integração da RAVENA com ferramentas # externas, como APIs de busca na web, calculadoras, serviços de mapas, etc. def integrar_ferramenta_externa(comando_ferramenta, parametros):     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_FERRAMENTA: Tentativa de integração com ferramenta: '{comando_ferramenta}' com parâmetros {parametros}.")     # Aqui, a lógica decidiria qual ferramenta chamar e como processar sua saída.     # Por exemplo, se 'comando_ferramenta' fosse 'pesquisar_google', faria uma chamada HTTP.     return "Integração com ferramentas externas ainda não implementada para este comando, ou a ferramenta solicitada não está disponível." print("BLOCO 25 — INTEGRAÇÃO COM FERRAMENTAS EXTERNAS carregado.") # ============================================================ # BLOCO 26 — GESTÃO DE PERSUASÃO E PERSONA (TO BE IMPLEMENTED) # ============================================================ # Este bloco ajustaria o estilo da resposta para se alinhar a uma persona definida # ou para otimizar a persuasão/engajamento com o usuário. def ajustar_persona_resposta(resposta, contexto_conversa):     if MODO_EXPLICACAO == "tecnico":         print("LOG_PERSONA: Ajustando persona da resposta.")     # Exemplos de ajustes:     # - Adicionar um tom mais formal/informal com base no histórico.     # - Incluir um emoji ou exclamação para engajamento.     # - Reformular frases para serem mais diretas ou mais explicativas.     return resposta # Por enquanto, retorna a resposta original sem modificação # ============================================================ # BLOCO 27 — AVALIAÇÃO DE DESEMPENHO E MÉTRICAS (TO BE IMPLEMENTED) # ============================================================ # Este bloco é essencial para monitorar e melhorar a RAVENA, registrando # métricas de desempenho como taxa de acerto, tempo de resposta, uso da # memória, e feedback do usuário. def registrar_metrica_desempenho(tipo_metrica, valor, detalhes={}):     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_METRICA: Registrando métrica '{tipo_metrica}': {valor} (Detalhes: {detalhes}).")     # Em uma implementação completa, esses dados seriam salvos em um banco de dados,     # um sistema de logs ou uma ferramenta de análise (e.g., Prometheus, Grafana).     # Exemplos de métricas:     # - 'taxa_acerto': percentual de respostas corretas.     # - 'tempo_resposta': latência da consulta.     # - 'feedback_positivo': contagem de feedbacks positivos.     pass # No momento, apenas imprime no log técnico def log_erro_resposta(pergunta_original, top_1_candidato, top_2_candidato, intencao_detectada):     """     Registra detalhes de uma resposta considerada incorreta, para análise e melhoria.     """     if MODO_EXPLICACAO == "tecnico":         print("LOG_ERRO_RESPOSTA: Registrando resposta incorreta.")         print(f"  Pergunta Original: {pergunta_original}")         print(f"  Top 1 Candidato: {top_1_candidato['item']['pergunta'] if top_1_candidato else 'N/A'} (Score: {top_1_candidato['score']:.2f} Categoria: {top_1_candidato['item']['categoria'] if top_1_candidato else 'N/A'})")         print(f"  Top 2 Candidato: {top_2_candidato['item']['pergunta'] if top_2_candidato else 'N/A'} (Score: {top_2_candidato['score']:.2f} Categoria: {top_2_candidato['item']['categoria'] if top_2_candidato else 'N/A'})")         print(f"  Intenção Detectada: {intencao_detectada}")     # Em uma implementação real, estes dados seriam persistidos em um log ou banco de dados     pass # ============================================================ # BLOCO 28 — ORQUESTRADOR PRINCIPAL (RAVENA_CORE) # ============================================================ # O Orquestrador Principal é o cérebro da RAVENA, coordenando o fluxo de # processamento de uma pergunta do início ao fim, chamando os outros blocos # na sequência correta. def ravena_core_process(pergunta_usuario):     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_ORQUESTRADOR: Recebida a pergunta pelo orquestrador principal: '{pergunta_usuario}'.")     resposta_final = ""     score_final = 0.0     match_item_id = "N/A"     pergunta_processada = pergunta_usuario # Valor inicial     try:         # 1. Processar Comandos Especiais (BLOCO 18)         resultado_comando = processar_comando_especial(pergunta_usuario)         if resultado_comando is not None:             resposta_final = resultado_comando             return resposta_final # Se for um comando especial, a resposta é gerada aqui         # A função `processar_pergunta` já inclui a resolução de anáforas e a busca de conhecimento         # e fallback, então `ravena_core_process` deve chamá-la diretamente.         resposta_completa_com_explicacao = processar_pergunta(pergunta_usuario)         # A `processar_pergunta` já retorna a resposta final formatada. Precisamos extrair a parte pura         # para compatibilidade com o logging de métricas e ajuste de persona, se necessário.         # A lógica para extrair a resposta pura será adaptada do que já existe nos testes.         if MODO_EXPLICACAO == "tecnico":             resposta_final = resposta_completa_com_explicacao.split('\n\n[Explicação:')[0].strip() # Tenta remover a explicação do processar_pergunta             if "[Explicação (Orquestrador):" in resposta_completa_com_explicacao: # Fallback para formato do orquestrador se processar_pergunta retorna um erro.                  resposta_final = resposta_completa_com_explicacao.split('\n\n[Explicação (Orquestrador):')[0].strip()         else:             resposta_final = resposta_completa_com_explicacao         # Recuperar score e id do item matched se disponível (para logging/metrics)         # Esta parte é mais complexa pois `processar_pergunta` retorna a string final.         # Para propósitos de logging de métricas no orquestrador, podemos ter que simplificar         # ou refatorar para que `processar_pergunta` retorne também metadados.         # Por enquanto, mantemos `score_final` e `match_item_id` como placeholders simples.         # O logging detalhado já ocorre dentro de `processar_pergunta` e `_buscar_conhecimento_interno`.         # 5. Ajustar Persona (BLOCO 26) - se implementado         resposta_final = ajustar_persona_resposta(resposta_final, obter_historico_conversa())     except Exception as e:         import traceback         error_details = f"{type(e).__name__}: {e}" if str(e) else type(e).__name__         resposta_final = lidar_com_excecao(pergunta_usuario, tipo_erro="execucao_orquestrador", detalhes=error_details)         if MODO_EXPLICACAO == "tecnico":             print(f"ERRO_ORQUESTRADOR: {error_details}")             print(f"TRACEBACK_ORQUESTRADOR:\n{traceback.format_exc()}") # Print full traceback     finally:         # 7. Registrar Métricas (BLOCO 27)         # Garante que as métricas sejam registradas mesmo em caso de erro.         # O score detalhado é logado dentro de _buscar_conhecimento_interno e processar_pergunta.         registrar_metrica_desempenho("consulta", 1, {             "pergunta_original": pergunta_usuario,             "pergunta_processada": pergunta_processada,             "score_final": score_final, # Placeholder, detalhe no log interno             "match_id": match_item_id,             "resposta_length": len(resposta_final)         })     # Prepara a saída com base no MODO_EXPLICACAO     # O `processar_pergunta` já faz a formatação para o MODO_EXPLICACAO. Retornamos seu output direto.     return resposta_completa_com_explicacao # ============================================================ # BLOCO 29 — INTERFACE E INTEGRAÇÃO (TO BE IMPLEMENTED) # ============================================================ # Este bloco seria a camada de interação com o usuário ou com outros sistemas. # Pode ser uma interface de linha de comando, uma API REST, ou uma integração # com um chatbot em outras plataformas. def iniciar_interface_cli():     print("\n--- RAVENA - Seu Assistente de Conhecimento ---")     print("Digite 'sair' para encerrar a conversa.")     print("Comandos especiais: 'aprenda: Pergunta = Resposta', 'correto: Pergunta', 'errado: Pergunta', 'esqueca: Pergunta'")     print("Modo de explicação: MODO_EXPLICACAO = 'tecnico' | 'simples' | 'oculto'")     while True:         pergunta = input("\nVocê: ")         if pergunta.lower() == 'sair':             print("Até logo!")             break         # Aqui o orquestrador principal é chamado         resposta = ravena_core_process(pergunta)         print(f"RAVENA: {resposta}") # Exemplo de como iniciar a interface: # iniciar_interface_cli() print("BLOCO 29 — INTERFACE E INTEGRAÇÃO carregado.") # ============================================================ # BLOCO 17 — GESTÃO DA MEMÓRIA DE CURTO PRAZO # ============================================================ def adicionar_memoria_curta(item_id, pergunta_usuario, resposta_gerada):     global memoria_curta     memoria_curta.appendleft({         'id': item_id,         'pergunta_usuario': pergunta_usuario,         'resposta_gerada': resposta_gerada     })     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_MEMORIA_CURTA: Adicionado à memória de curto prazo: ID {item_id[:8]} - '{pergunta_usuario[:30]}...' (Resposta: {resposta_gerada[:30]}...)") def limpar_memoria_curta():     global memoria_curta     memoria_curta.clear()     if MODO_EXPLICACAO == "tecnico":         print("LOG_MEMORIA_CURTA: Memória de curto prazo limpa.") def obter_historico_conversa(n=LIMITE_MEMORIA_CURTA):     global memoria_curta     return list(memoria_curta)[:n] print("BLOCO 17 — GESTÃO DA MEMÓRIA DE CURTO PRAZO carregado.") def auto_aprender(pergunta_original, resposta_gerada, score_match):     # Não aprende perguntas vazias ou respostas muito curtas     if not pergunta_original or not resposta_gerada or len(resposta_gerada) < MIN_COMPRIMENTO_RESPOSTA_AUTO_APRENDER:         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_AUTO_LEARN: Não auto-aprendendo: pergunta/resposta inválida ou muito curta. Comprimento: {len(resposta_gerada)}")         return     # Usa o score_match recebido, se for relevante, para decidir se vale a pena aprender     # A lógica aqui pode ser mais complexa, considerando a confiança do sistema     # e a 'novidade' da pergunta.     # Simulação de pontuação para auto-aprendizagem     pontuacao_auto_aprender = score_match # A pontuação já vem do 'score_final' da decisão     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_AUTO_LEARN: Pontuação={pontuacao_auto_aprender:.2f}")     # A decisão de pode_aprender é feita externamente, aqui apenas executa o aprendizado     # Classificar a pergunta para determinar a categoria e intenção     # Usando a nova função multi-categoria, mas pegando a de maior score para o aprendizado     pergunta_embedding = gerar_embedding(pergunta_original)     categorias_detectadas = detectar_categorias_multiplas(pergunta_original, pergunta_embedding)     categoria_prevista = categorias_detectadas[0][0] if categorias_detectadas else 'geral'     intencao_prevista = detectar_intencao(pergunta_original)     # Verificar se já existe um conhecimento muito similar para evitar duplicação exata     # Uma busca rápida com um threshold mais alto apenas para esta verificação     memoria_para_verificar = carregar_conhecimento(categoria_prevista)     similaridade_existente = 0     id_existente = None     for item in memoria_para_verificar:         sim = calcular_similaridade_cosseno(pergunta_embedding, item['embedding_pergunta'])         if sim > similaridade_existente:             similaridade_existente = sim             id_existente = item['id']     if similaridade_existente >= UPDATE_EXISTING_KNOWLEDGE_THRESHOLD: # Threshold alto para considerar o mesmo item         # Atualizar o item existente com a nova resposta ou aumentar a confiança/peso         item_existente = obter_conhecimento_por_id(id_existente)         if item_existente:             # Lógica para sintetizar respostas ou apenas atualizar meta-dados             item_existente['confianca'] = min(1.0, item_existente.get('confianca', 0.8) + 0.1)             item_existente['peso'] = min(PESO_MAX, item_existente.get('peso', 1.0) + 0.1)             item_existente['acessos'] = item_existente.get('acessos', 0) + 1             atualizar_conhecimento(id_existente,                                    nova_pergunta=item_existente['pergunta'],                                    nova_resposta=item_existente['resposta'],                                    nova_categoria=categoria_prevista,                                    nova_intencao=intencao_prevista)             if MODO_EXPLICACAO == "tecnico":                 print(f"LOG_AUTO_LEARN: Item existente '{item_existente['pergunta']}' atualizado. ID: {id_existente[:8]}")         return     # Se não houver um item similar o suficiente, adicionar como novo     adicionar_conhecimento(pergunta_original, resposta_gerada, categoria_prevista, intencao_prevista)     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_AUTO_LEARN: Novo conhecimento aprendido de forma autônoma: '{pergunta_original}' na categoria '{categoria_prevista}'.") def feedback(item_id, positivo=True):     item = obter_conhecimento_por_id(item_id)     if item:         if positivo:             item['confianca'] = min(1.0, item.get('confianca', 0.5) + 0.15) # Aumenta confiança             item['peso'] = min(PESO_MAX, item.get('peso', 1.0) + 0.5)      # Aumenta peso         else:             item['confianca'] = max(0.0, item.get('confianca', 0.5) - 0.2) # Diminui confiança             item['peso'] = max(PESO_MIN, item.get('peso', 1.0) - 0.2)      # Diminui peso         atualizar_conhecimento(item_id,                                nova_pergunta=item['pergunta'],                                nova_resposta=item['resposta'],                                nova_categoria=item['categoria'],                                nova_intencao=item['intencao'])         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_FEEDBACK: Feedback {'positivo' if positivo else 'negativo'} para ID {item_id[:8]}. Confiança: {item['confianca']:.2f}, Peso: {item['peso']:.2f}") def decair_confianca_e_peso():     global BASE_EMBEDDINGS     # Esta função seria chamada periodicamente (e.g., a cada N interações ou tempo)     for item_id, item in list(BASE_EMBEDDINGS.items()): # Usar list() para evitar RuntimeError: dictionary changed size during iteration         if item['acessos'] == 0: # Itens nunca acessados decaem mais rápido ou são removidos              item['confianca'] = max(0.0, item.get('confianca', 0.5) - (PESOS['decay_rate_confidence'] * 2))              item['peso'] = max(PESO_MIN, item.get('peso', 1.0) - (PESOS['decay_rate_weight'] * 2))         else:             item['confianca'] = max(0.0, item.get('confianca', 0.5) - PESOS['decay_rate_confidence'])             item['peso'] = max(PESO_MIN, item.get('peso', 1.0) - PESOS['decay_rate_weight'])         if item['confianca'] < LOW_CONFIDENCE * 0.5 and item['acessos'] == 0: # Remover itens com confiança muito baixa e não acessados             remover_conhecimento(item_id)             if MODO_EXPLICACAO == "tecnico":                 print(f"LOG_DECAY: Item ID {item_id[:8]} removido por baixa confiança e falta de acesso.")         elif item['confianca'] <= 0 or item['peso'] <= PESO_MIN * 0.5: # Remover se confiança ou peso chegar a zero             remover_conhecimento(item_id)             if MODO_EXPLICACAO == "tecnico":                 print(f"LOG_DECAY: Item ID {item_id[:8]} removido por confiança/peso zero.")         else:             # Salvar as alterações de confiança/peso             atualizar_conhecimento(item_id,                                    nova_pergunta=item['pergunta'],                                    nova_resposta=item['resposta'],                                    nova_categoria=item['categoria']) def detectar_categorias_multiplas(pergunta, embedding):     global CATEGORIA_MIN_SCORE     resultados = []     # Usando DESCRICOES_CATEGORIA e categorias_dados para obter a descrição     # A DESCRICOES_CATEGORIA não está definida globalmente no notebook, então vou recriar aqui para evitar erro.     # É melhor usar `categorias_dados` diretamente.     descricoes_categoria_map = {cat['nome']: cat['descricao'] for cat in categorias_dados}     for cat_nome, cat_desc in descricoes_categoria_map.items():         emb_cat = gerar_embedding(cat_desc)         score = calcular_similaridade_cosseno(embedding, emb_cat)         resultados.append((cat_nome, score))     resultados.sort(key=lambda x: x[1], reverse=True)     # Always include the top category, regardless of its score, to ensure a search is attempted.     # Then include other categories if they meet the CATEGORIA_MIN_SCORE.     categorias_para_busca = []     if resultados:         # Always add the top scoring category         categorias_para_busca.append(resultados[0])         # Add other categories if they meet the minimum score         for cat_name, score in resultados[1:3]: # Consider top 3             if score > CATEGORIA_MIN_SCORE:                 categorias_para_busca.append((cat_name, score))     if not categorias_para_busca: # Should ideally not happen if resultados is not empty         if MODO_EXPLICACAO == "tecnico":             print("LOG_CATEGORIA: Nenhuma categoria para busca após filtragem. Usando 'fallback_controlado'.")         return [("fallback_controlado", 0.0)]     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_CATEGORIA: Categorias selecionadas para busca: {categorias_para_busca}")     return categorias_para_busca def buscar_por_categoria(categoria, embedding):     resultados = []     # Iterar sobre BASE_EMBEDDINGS (que contém todos os itens carregados)     # e filtrar pela categoria desejada.     for item_id, item_data in BASE_EMBEDDINGS.items():         if item_data['categoria'] == categoria:             # Certificar-se de que o embedding da pergunta está em formato numpy array             item_embedding = item_data['embedding_pergunta']             if not isinstance(item_embedding, np.ndarray):                 item_embedding = np.array(item_embedding, dtype=np.float64)             score = calcular_similaridade_cosseno(embedding, item_embedding)             if score > SEMANTIC_THRESHOLD: # Usar o SEMANTIC_THRESHOLD global                 # Retorna o item completo junto com o score base                 resultados.append({                     **item_data, # Inclui todos os dados do item                     'score_base': score                 })     return resultados def processar_pergunta(pergunta):     global MODO_EXPLICACAO, memoria_curta     # 1. Processar Comandos Especiais     resultado_comando = processar_comando_especial(pergunta)     if resultado_comando is not None:         return resultado_comando     # 2. Gerar Embedding da pergunta do usuário     embedding_pergunta_usuario = gerar_embedding(pergunta)     # 3. DETECÇÃO MULTI-CATEGORIA     categorias = detectar_categorias_multiplas(pergunta, embedding_pergunta_usuario)     # 4. BUSCA HÍBRIDA (Obter candidatos de todas as categorias relevantes)     candidatos_brutos = []     for cat, confianca_cat in categorias:         candidatos_cat = buscar_por_categoria(cat, embedding_pergunta_usuario)         candidatos_brutos.extend(candidatos_cat)     # 5. Remover Duplicados     candidatos = remover_duplicados(candidatos_brutos)     if not candidatos:         # Fallback inteligente se não houver candidatos mesmo após busca híbrida         if MODO_EXPLICACAO == "tecnico":             print("LOG_ORQUESTRADOR: Nenhum candidato encontrado após busca multi-categoria. Acionando fallback inteligente.")         return fallback_inteligente(pergunta, [])     # 6. RERANQUEAMENTO COM VALIDAÇÃO     candidatos_ranqueados = reranquear_candidatos(pergunta, embedding_pergunta_usuario, candidatos)     # 7. Selecionar o Melhor Candidato e Validar Resposta     melhor_match = candidatos_ranqueados[0] if candidatos_ranqueados else None     if validar_resposta(melhor_match):         resposta = melhor_match['resposta']         item_id = melhor_match['id']         # Atualizar acessos (se necessário, já pode estar no rerank ou no CRUD)         item_to_update = obter_conhecimento_por_id(item_id)         if item_to_update:             item_to_update['acessos'] = item_to_update.get('acessos', 0) + 1             # A atualização de embeddings e outros metadados é feita no CRUD se for uma atualização completa             # Aqui, apenas incrementamos 'acessos' em memória; para persistir, chamar atualizar_conhecimento             atualizar_conhecimento(item_id, item_to_update['pergunta'], item_to_update['resposta'], item_to_update['categoria'], item_to_update['intencao'])         # Adiciona a interação à memória de curto prazo         adicionar_memoria_curta(item_id, pergunta, resposta)         if MODO_EXPLICACAO == "tecnico":             explicacao = f"\n\n[Explicação:\nTipo: {melhor_match['categoria']} | Intenção: {melhor_match['intencao']} | Confiança: {melhor_match['confianca']:.2f} | Peso: {melhor_match['peso']:.2f} | Acessos: {melhor_match['acessos']} | Score: {melhor_match['score_final']:.2f} | ]"             return resposta + explicacao         elif MODO_EXPLICACAO == "simples":             if melhor_match['confianca'] < HIGH_CONFIDENCE:                 return resposta + " (Minha confiança nesta resposta é média.)"             return resposta         else: # oculto             return resposta     else:         # 8. FALLBACK INTELIGENTE (se a validação falhar)         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_ORQUESTRADOR: Validação de resposta falhou para o melhor match. Score final: {melhor_match['score_final']:.2f}. Acionando fallback inteligente.")         return fallback_inteligente(pergunta, candidatos_ranqueados) def auto_aprender(pergunta_original, resposta_gerada, score_match):     # Não aprende perguntas vazias ou respostas muito curtas     if not pergunta_original or not resposta_gerada or len(resposta_gerada) < MIN_COMPRIMENTO_RESPOSTA_AUTO_APRENDER:         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_AUTO_LEARN: Não auto-aprendendo: pergunta/resposta inválida ou muito curta. Comprimento: {len(resposta_gerada)}")         return     # Usa o score_match recebido, se for relevante, para decidir se vale a pena aprender     # A lógica aqui pode ser mais complexa, considerando a confiança do sistema     # e a 'novidade' da pergunta.     # Simulação de pontuação para auto-aprendizagem     pontuacao_auto_aprender = score_match + (len(resposta_gerada) / 100.0) # Exemplo simples     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_AUTO_LEARN: Pontuação={pontuacao_auto_aprender:.2f}")     if PONTUACAO_MIN_AUTO_APRENDER <= pontuacao_auto_aprender <= PONTUACAO_MAX_AUTO_APRENDER:         # O sistema decide que esta é uma boa oportunidade para aprender ou refinar         # Aqui, vamos adicionar como um novo conhecimento, mas uma lógica mais avançada         # poderia tentar encontrar um item existente para atualizar         # Classificar a pergunta para determinar a categoria e intenção         categoria_prevista, categoria_score = classificar_tipo_pergunta(pergunta_original)         intencao_prevista = classificar_intencao(pergunta_original)         # Verificar se já existe um conhecimento muito similar para evitar duplicação exata         # Uma busca rápida com um threshold mais alto apenas para esta verificação         memoria_para_verificar = carregar_conhecimento(categoria_prevista)         similaridade_existente = 0         id_existente = None         pergunta_embedding = gerar_embedding(pergunta_original)         for item in memoria_para_verificar:             sim = calcular_similaridade_cosseno(pergunta_embedding, item['embedding_pergunta'])             if sim > similaridade_existente:                 similaridade_existente = sim                 id_existente = item['id']         if similaridade_existente >= UPDATE_EXISTING_KNOWLEDGE_THRESHOLD: # Threshold alto para considerar o mesmo item             # Atualizar o item existente com a nova resposta ou aumentar a confiança/peso             item_existente = obter_conhecimento_por_id(id_existente)             if item_existente:                 # Lógica para sintetizar respostas ou apenas atualizar meta-dados                 item_existente['confianca'] = min(1.0, item_existente.get('confianca', 0.8) + 0.1)                 item_existente['peso'] = min(PESO_MAX, item_existente.get('peso', 1.0) + 0.1)                 item_existente['acessos'] = item_existente.get('acessos', 0) + 1                 atualizar_conhecimento(id_existente,                                        nova_pergunta=item_existente['pergunta'],                                        nova_resposta=item_existente['resposta'],                                        nova_categoria=categoria_prevista,                                        nova_intencao=intencao_prevista)                 if MODO_EXPLICACAO == "tecnico":                     print(f"LOG_AUTO_LEARN: Item existente '{item_existente['pergunta']}' atualizado. ID: {id_existente[:8]}")             return         # Se não houver um item similar o suficiente, adicionar como novo         adicionar_conhecimento(pergunta_original, resposta_gerada, categoria_prevista, intencao_prevista)         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_AUTO_LEARN: Novo conhecimento aprendido de forma autônoma: '{pergunta_original}' na categoria '{categoria_prevista}'.") def feedback(item_id, positivo=True):     item = obter_conhecimento_por_id(item_id)     if item:         if positivo:             item['confianca'] = min(1.0, item.get('confianca', 0.5) + 0.15) # Aumenta confiança             item['peso'] = min(PESO_MAX, item.get('peso', 1.0) + 0.5)      # Aumenta peso         else:             item['confianca'] = max(0.0, item.get('confianca', 0.5) - 0.2) # Diminui confiança             item['peso'] = max(PESO_MIN, item.get('peso', 1.0) - 0.2)      # Diminui peso         atualizar_conhecimento(item_id,                                nova_pergunta=item['pergunta'],                                nova_resposta=item['resposta'],                                nova_categoria=item['categoria'],                                nova_intencao=item['intencao'])         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_FEEDBACK: Feedback {'positivo' if positivo else 'negativo'} para ID {item_id[:8]}. Confiança: {item['confianca']:.2f}, Peso: {item['peso']:.2f}") def decair_confianca_e_peso():     global BASE_EMBEDDINGS     # Esta função seria chamada periodicamente (e.g., a cada N interações ou tempo)     for item_id, item in list(BASE_EMBEDDINGS.items()): # Usar list() para evitar RuntimeError: dictionary changed size during iteration         if item['acessos'] == 0: # Itens nunca acessados decaem mais rápido ou são removidos              item['confianca'] = max(0.0, item.get('confianca', 0.5) - (PESOS['decay_rate_confidence'] * 2))              item['peso'] = max(PESO_MIN, item.get('peso', 1.0) - (PESOS['decay_rate_weight'] * 2))         else:             item['confianca'] = max(0.0, item.get('confianca', 0.5) - PESOS['decay_rate_confidence'])             item['peso'] = max(PESO_MIN, item.get('peso', 1.0) - PESOS['decay_rate_weight'])         if item['confianca'] < LOW_CONFIDENCE * 0.5 and item['acessos'] == 0: # Remover itens com confiança muito baixa e não acessados             remover_conhecimento(item_id)             if MODO_EXPLICACAO == "tecnico":                 print(f"LOG_DECAY: Item ID {item_id[:8]} removido por baixa confiança e falta de acesso.")         elif item['confianca'] <= 0 or item['peso'] <= PESO_MIN * 0.5: # Remover se confiança ou peso chegar a zero             remover_conhecimento(item_id)             if MODO_EXPLICACAO == "tecnico":                 print(f"LOG_DECAY: Item ID {item_id[:8]} removido por confiança/peso zero.")         else:             # Salvar as alterações de confiança/peso             atualizar_conhecimento(item_id,                                    nova_pergunta=item['pergunta'],                                    nova_resposta=item['resposta'],                                    nova_categoria=item['categoria']) def _buscar_conhecimento_interno(pergunta_original):     global memoria_curta     pergunta_processada = preprocessar_texto(pergunta_original, remover_stopwords=True, lematizar=True)     pergunta_embedding = gerar_embedding(pergunta_original)     # 1. Classificar Categoria da pergunta     resultado_deteccao, nivel_confianca = classificar_tipo_pergunta(pergunta_original)     categoria_prevista = resultado_deteccao["categoria"]     confianca_deteccao = resultado_deteccao["confianca"]     # 2. Detectar Intenção da pergunta     intencao_prevista = detectar_intencao(pergunta_original)     usar_busca_global = False     categoria_para_buscar = None     if nivel_confianca == "alta_confianca":         categoria_para_buscar = categoria_prevista         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_BUSCAR: Categoria '{categoria_para_buscar}' com alta confiança ({confianca_deteccao:.2f}).")     elif nivel_confianca == "media_confianca":         categoria_para_buscar = categoria_prevista         usar_busca_global = True # Consider global search for medium confidence         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_BUSCAR: Categoria '{categoria_para_buscar}' com média confiança ({confianca_deteccao:.2f}), habilitando busca global.")     else: # baixa_confianca         categoria_para_buscar = None # This will trigger global search below         usar_busca_global = True # Always use global search for low confidence         if MODO_EXPLICACAO == "tecnico":             print(f"LOG_BUSCAR: Baixa confiança na classificação ({confianca_deteccao:.2f}), forçando busca global.")     # 3. Obter todos os itens de conhecimento da categoria prevista (e global se necessário)     memoria_relevante = []     if categoria_para_buscar:         memoria_relevante.extend(carregar_conhecimento(categoria_para_buscar))     if usar_busca_global or not categoria_para_buscar:         if MODO_EXPLICACAO == "tecnico":             print("LOG_BUSCAR: Executando busca em todas as categorias.")         todas_memorias = []         for cat_info in categorias_dados:             todas_memorias.extend(carregar_conhecimento(cat_info['nome']))         memoria_relevante_temp = []         ids_ja_adicionados = set()         for item in memoria_relevante:             memoria_relevante_temp.append(item)             ids_ja_adicionados.add(item['id'])         for item in todas_memorias:             if item['id'] not in ids_ja_adicionados:                 memoria_relevante_temp.append(item)                 ids_ja_adicionados.add(item['id'])         memoria_relevante = memoria_relevante_temp     candidatos_com_scores_base = []     if MODO_EXPLICACAO == "tecnico":         print(f"LOG_BUSCAR: '{pergunta_processada}' | int='{intencao_prevista}'")     if not memoria_relevante:         if MODO_EXPLICACAO == "tecnico":             print("LOG_BUSCAR: Nenhum candidato encontrado após filtragem de categoria.")         # Retorna o contexto de detecção para as novas funções de fallback         return [], categoria_prevista, confianca_deteccao, intencao_prevista     # Tentar busca por cluster se houver um modelo KMeans treinado e itens suficientes     cluster_match_found = False     if 'kmeans_model' in globals() and 'cluster_labels' in globals() and len(memoria_relevante) >= MIN_ITENS_CLUSTER:         try:             # Prever o cluster da pergunta             pergunta_cluster = kmeans_model.predict(pergunta_embedding.reshape(1, -1))[0]             # Obter os IDs dos itens que pertencem a esse cluster             ids_no_cluster = [item_id for item_id, cluster_id in cluster_labels.items() if cluster_id == pergunta_cluster]             if MODO_EXPLICACAO == "tecnico":                 print(f"DEBUG_CLUSTER: Pergunta no Cluster {pergunta_cluster}. Itens no cluster: {len(ids_no_cluster)}.")             # Filtrar memoria_relevante para incluir apenas itens do cluster previsto             memoria_filtrada_por_cluster = [item for item in memoria_relevante if item['id'] in ids_no_cluster]             if memoria_filtrada_por_cluster:                 memoria_relevante = memoria_filtrada_por_cluster                 cluster_match_found = True                 if MODO_EXPLICACAO == "tecnico":                     print("LOG_CLUSTER: Match no cluster encontrado. Filtrando itens por cluster.")             else:                 if MODO_EXPLICACAO == "tecnico":                     print("LOG_CLUSTER: Nenhum item no cluster correspondente. Revertendo para busca em todos os itens.")         except Exception as e:             if MODO_EXPLICACAO == "tecnico":                 print(f"WARN_CLUSTER: Erro ao usar cluster para busca: {e}. Prosseguindo sem cluster.")             cluster_match_found = False # Resetar em caso de erro     for item_conhecimento in memoria_relevante:         # Preprocessar a pergunta do item para fuzzy matching         pergunta_item_descrip = item_conhecimento['pergunta']         pergunta_item_processada = preprocessar_texto(pergunta_item_descrip, remover_stopwords=True, lematizar=True)         # Calcular similaridade semântica (embeddings)         sim_semantica_pergunta = calcular_similaridade_cosseno(pergunta_embedding, item_conhecimento['embedding_pergunta'])         # Calcular similaridade fuzzy (string)         sim_fuzzy = calcular_similaridade_fuzzy(pergunta_processada, pergunta_item_processada)         # Combinar scores         score_base = (sim_semantica_pergunta * PESOS['semantic']) + (sim_fuzzy * PESOS['fuzzy'])         # Adicionar o bônus de cluster ao score base, se aplicável         if cluster_match_found:             score_base += 0.05 # Pequeno bônus por ter encontrado o item no cluster correto         candidatos_com_scores_base.append({"item": item_conhecimento, "score": score_base})     # Retorna os candidatos brutos para filtragem e reranking nas próximas etapas do pipeline     return candidatos_com_scores_base, categoria_prevista, confianca_deteccao, intencao_prevista def classificar_tipo_pergunta(pergunta):     # Prepare categories in the format required by detectar_categoria     categorias_dict = {cat["nome"]: cat["descricao"] for cat in categorias_dados}     # Use detectar_categoria to get classification result     resultado_deteccao = detectar_categoria(pergunta, categorias_dict, gerar_embedding)     melhor_categoria = resultado_deteccao["categoria"]     maior_score_final = resultado_deteccao["score"]     confianca_deteccao = resultado_deteccao["confianca"]     # Use decidir_categoria to determine the confidence level     nivel_confianca = decidir_categoria(resultado_deteccao)     if MODO_EXPLICACAO == "tecnico":         print(f"DEBUG_RAVENA: Núcleo='{extrair_palavras_chave(pergunta)}' → Cat='{melhor_categoria}' (Score={maior_score_final:.4f}) [Confiança: {confianca_deteccao:.4f} | Nível: {nivel_confianca}]")         print("DEBUG_CATEGORIA:", resultado_deteccao)     return resultado_deteccao, nivel_confianca # ============================================================ # BLOCO 13 — CLASSIFICAÇÃO DE INTENÇÕES # ============================================================ # Este bloco não contém mais as funções de detecção de intenção, # que foram movidas para o BLOCO 9 (Utilidades de Texto) # e refatoradas para o novo pipeline baseado em padrões. import numpy as np from sklearn.metrics.pairwise import cosine_similarity # ============================================================ # BLOCO 30 — DIAGNÓSTICO DE FALLBACK # ============================================================ def fallback_diagnostico(pergunta, candidatos, contexto):     diagnostico = {         "erro": True,         "motivo": None,         "detalhes": {}     }     top1 = candidatos[0] if candidatos else None     # =========================     # 1. SCORE BAIXO     # =========================     if not top1 or top1["score"] < 0.75:         diagnostico["motivo"] = "baixa_similaridade"     # =========================     # 2. INTENÇÃO ERRADA     # =========================     if top1:         # Assuming 'tipo' is the standardized intention of the knowledge item         # and 'intencao' is the detected intention of the user's question         if top1["item"].get("intencao") != contexto["intencao"]:             diagnostico["motivo"] = "intencao_incorreta"     # =========================     # 3. CATEGORIA SUSPEITA     # =========================     # 'conf_cat' is the confidence score for category classification     if contexto["confianca_categoria"] < 0.4:         diagnostico["motivo"] = "categoria_insegura"     # =========================     # 4. POSSÍVEL ERRO DE DADOS (criptografia etc)     # =========================     if top1 and (top1["item"]["resposta"] is None or top1["item"]["pergunta"] is None):         diagnostico["motivo"] = "erro_dado_memoria"     # =========================     # LOG ESTRUTURADO (placeholder function, assuming it will be defined elsewhere)     # =========================     # For now, let's just print to console if in technical mode     if MODO_EXPLICACAO == "tecnico":         print("LOG_DIAGNOSTICO:", {             "pergunta": pergunta,             "top1": top1["item"]["pergunta"] if top1 else "N/A",             "top3_perguntas": [c["item"]["pergunta"] for c in candidatos[:3]],             "contexto_intencao": contexto["intencao"],             "contexto_conf_cat": contexto["confianca_categoria"],             "diagnostico": diagnostico         })     return {         "resposta": "Ainda não tenho certeza suficiente.",         "debug": diagnostico     } def analisar_logs_erros(logs):     contagem = {}     for log in logs:         # Assuming 'diagnostico' key is present in each log entry         if "diagnostico" in log and "motivo" in log["diagnostico"]:             motivo = log["diagnostico"]["motivo"]             contagem[motivo] = contagem.get(motivo, 0) + 1         else:             # Handle cases where log entry might not have the expected structure             contagem["UNKNOWN_LOG_FORMAT"] = contagem.get("UNKNOWN_LOG_FORMAT", 0) + 1     return contagem print("BLOCO 30 — DIAGNÓSTICO DE FALLBACK carregado.") # ============================================================ # BLOCO 12 — GESTÃO DO CONHECIMENTO (CRUD) # ============================================================ def adicionar_conhecimento(pergunta, resposta, categoria='geral', intencao='geral'):     global BASE_EMBEDDINGS, MEMORY_EMBEDDINGS     # Gerar embeddings para a nova pergunta e resposta     embedding_pergunta = gerar_embedding(pergunta)     embedding_resposta = gerar_embedding(resposta)     novo_item = {         'id': str(uuid.uuid4()),         'pergunta': pergunta,         'resposta': resposta,         'embedding_pergunta': embedding_pergunta.tolist(), # Salvar como lista para JSON         'embedding_resposta': embedding_resposta.tolist(), # Salvar como lista para JSON         'categoria': categoria,         'intencao': intencao,         'confianca': 1.0, # Novo item começa com confiança total         'peso': 1.0,      # Novo item começa com peso normal         'acessos': 0      # Novo item começa sem acessos     }     memoria_categoria = carregar_conhecimento(categoria)     memoria_categoria.append(novo_item)     salvar_conhecimento(memoria_categoria, categoria)     # Recarregar na memória global (garante que os embeddings estejam como numpy arrays)     novo_item_recarregado = novo_item.copy() # Criar uma cópia para evitar modificar o original     novo_item_recarregado['embedding_pergunta'] = np.array(novo_item_recarregado['embedding_pergunta'])     novo_item_recarregado['embedding_resposta'] = np.array(novo_item_recarregado['embedding_resposta'])     BASE_EMBEDDINGS[novo_item_recarregado['id']] = novo_item_recarregado     MEMORY_EMBEDDINGS[novo_item_recarregado['id']] = novo_item_recarregado['embedding_pergunta']     print(f"Conhecimento adicionado/atualizado na categoria '{categoria}'.")     return novo_item def obter_conhecimento_por_id(item_id):     return BASE_EMBEDDINGS.get(item_id) def atualizar_conhecimento(item_id, nova_pergunta=None, nova_resposta=None, nova_categoria=None, nova_intencao=None):     global BASE_EMBEDDINGS, MEMORY_EMBEDDINGS     item = BASE_EMBEDDINGS.get(item_id)     if not item:         print(f"Item com ID {item_id} não encontrado.")         return None     # Manter a categoria original caso não seja alterada     categoria_antiga = item['categoria']     categoria_nova = nova_categoria if nova_categoria else categoria_antiga     if nova_pergunta:         item['pergunta'] = nova_pergunta         item['embedding_pergunta'] = gerar_embedding(nova_pergunta)     if nova_resposta:         item['resposta'] = nova_resposta         item['embedding_resposta'] = gerar_embedding(nova_resposta)     if nova_intencao:         item['intencao'] = nova_intencao     if nova_categoria and nova_categoria != categoria_antiga:         # Remover da categoria antiga         memoria_antiga = carregar_conhecimento(categoria_antiga)         memoria_antiga = [i for i in memoria_antiga if i['id'] != item_id]         salvar_conhecimento(memoria_antiga, categoria_antiga)         # Adicionar à nova categoria         item['categoria'] = nova_categoria         memoria_nova = carregar_conhecimento(nova_categoria)         memoria_nova.append(item)         salvar_conhecimento(memoria_nova, nova_categoria)     else:         # Se a categoria não mudou, apenas salva na categoria existente         memoria_categoria = carregar_conhecimento(categoria_nova)         for i, stored_item in enumerate(memoria_categoria):             if stored_item['id'] == item_id:                 memoria_categoria[i] = item                 break         salvar_conhecimento(memoria_categoria, categoria_nova)     # Atualizar embeddings na memória global     BASE_EMBEDDINGS[item_id] = item     MEMORY_EMBEDDINGS[item_id] = item['embedding_pergunta']     print(f"Conhecimento com ID {item_id} atualizado na categoria '{categoria_nova}'.")     return item def remover_conhecimento(item_id):     global BASE_EMBEDDINGS, MEMORY_EMBEDDINGS     item = BASE_EMBEDDINGS.get(item_id)     if not item:         print(f"Item com ID {item_id} não encontrado.")         return False     categoria = item['categoria']     memoria_categoria = carregar_conhecimento(categoria)     memoria_categoria = [i for i in memoria_categoria if i['id'] != item_id]     salvar_conhecimento(memoria_categoria, categoria)     del BASE_EMBEDDINGS[item_id]     del MEMORY_EMBEDDINGS[item_id]     print(f"Conhecimento com ID {item_id} removido da categoria '{categoria}'.")     return True import os, json, re, unicodedata, shutil, time, uuid, ast import numpy as np import spacy import nltk from sentence_transformers import SentenceTransformer from sklearn.metrics.pairwise import cosine_similarity from sklearn.cluster import KMeans from cryptography.fernet import Fernet from collections import deque from nltk.corpus import stopwords from nltk.stem import RSLPStemmer from nltk.tokenize import word_tokenize try:     from fuzzywuzzy import fuzz except ImportError:     import subprocess     subprocess.run(["pip", "install", "fuzzywuzzy", "python-Levenshtein"])     from fuzzywuzzy import fuzz # ============================================================ # BLOCO 11 — ESTRUTURA E MANIPULAÇÃO DA MEMÓRIA # ============================================================ # Variáveis globais para armazenar embeddings para acesso rápido CATEGORY_EMBEDDINGS = {} MEMORY_EMBEDDINGS = {} BASE_EMBEDDINGS = {} def _obter_caminho_categoria(categoria):     return os.path.join(CONHECIMENTO_BASE_PATH, categoria + '.json') def carregar_conhecimento(categoria):     caminho_arquivo = _obter_caminho_categoria(categoria)     if os.path.exists(caminho_arquivo):         try:             with open(caminho_arquivo, 'r', encoding='utf-8') as f:                 dados_criptografados = json.load(f)                 # Descriptografa os itens ao carregar                 conhecimento_descriptografado = [                     {                         'id': item['id'],                         'pergunta': descriptografar(item['pergunta']),                         'resposta': descriptografar(item['resposta']),                         'embedding_pergunta': item['embedding_pergunta'],                         'embedding_resposta': item['embedding_resposta'],                         'categoria': item.get('categoria', categoria), # Mantém compatibilidade                         'intencao': item.get('intencao', 'geral'), # Mantém compatibilidade                         'tipo': detectar_intencao(descriptografar(item['pergunta'])), # CORRIGIDO: Usar detectar_intencao                         'confianca': item.get('confianca', 1.0),                         'peso': item.get('peso', 1.0),                         'acessos': item.get('acessos', 0)                     }                     for item in dados_criptografados                 ]                 return conhecimento_descriptografado         except json.JSONDecodeError as e:             print(f"ERRO: Arquivo de conhecimento '{caminho_arquivo}' está corrompido ou malformado JSON: {e}")             print(f"O arquivo será tratado como vazio. Considere inspecionar/remover o arquivo manualmente se o problema persistir.")             return [] # Trata o arquivo corrompido como se estivesse vazio     return [] def salvar_conhecimento(memoria, categoria):     caminho_arquivo = _obter_caminho_categoria(categoria)     os.makedirs(os.path.dirname(caminho_arquivo), exist_ok=True)     # Criptografa os itens antes de salvar     conhecimento_criptografado = [         {             'id': item['id'],             'pergunta': criptografar(item['pergunta']),             'resposta': criptografar(item['resposta']),             'embedding_pergunta': item['embedding_pergunta'].tolist() if isinstance(item['embedding_pergunta'], np.ndarray) else item['embedding_pergunta'], # Convert to list             'embedding_resposta': item['embedding_resposta'].tolist() if isinstance(item['embedding_resposta'], np.ndarray) else item['embedding_resposta'], # Convert to list             'categoria': item['categoria'],             'intencao': item['intencao'],             'confianca': item['confianca'],             'peso': item['peso'],             'acessos': item['acessos']         }         for item in memoria     ]     with open(caminho_arquivo, 'w', encoding='utf-8') as f:         json.dump(conhecimento_criptografado, f, ensure_ascii=False, indent=2) def carregar_todas_as_memorias():     todas_memorias = []     for categoria_info in categorias_dados:         categoria = categoria_info['nome']         todas_memorias.extend(carregar_conhecimento(categoria))     return todas_memorias # MODIFIED: to store more detailed embeddings for category classification def atualizar_embedding_categoria(categoria_nome):     global CATEGORY_EMBEDDINGS     # Find the full category info from categorias_dados list     categoria_info = next((cat for cat in categorias_dados if cat['nome'] == categoria_nome), None)     if categoria_info:         name_embedding = gerar_embedding(categoria_info['nome'])         desc_embedding = gerar_embedding(categoria_info['descricao'])         # Ensure examples list is not empty before generating embeddings         examples_embeddings = []         if categoria_info['exemplos']:             examples_embeddings = gerar_embedding(categoria_info['exemplos'])         CATEGORY_EMBEDDINGS[categoria_nome] = {             'name_embedding': name_embedding,             'desc_embedding': desc_embedding,             'examples_embeddings': examples_embeddings         } def inicializar_embeddings_categorias():     global CATEGORY_EMBEDDINGS     print("Inicializando embeddings de categorias...")     CATEGORY_EMBEDDINGS = {}     for categoria_info in categorias_dados:         atualizar_embedding_categoria(categoria_info['nome'])     print(f"{len(CATEGORY_EMBEDDINGS)} embeddings de categorias carregados.") def recarregar_base_de_conhecimento():     global BASE_EMBEDDINGS     global MEMORY_EMBEDDINGS     print("Recarregando base de conhecimento e embeddings...")     BASE_EMBEDDINGS.clear()     MEMORY_EMBEDDINGS.clear()     for categoria_info in categorias_dados:         categoria = categoria_info['nome']         conhecimento = carregar_conhecimento(categoria)         for item in conhecimento:             # Assegura que o embedding seja um numpy array, caso tenha sido desserializado como lista             # E explicitamente converte para float64 para evitar erros de dtype mismatch             if isinstance(item['embedding_pergunta'], list):                 item['embedding_pergunta'] = np.array(item['embedding_pergunta'], dtype=np.float64)             else:                 item['embedding_pergunta'] = item['embedding_pergunta'].astype(np.float64)             if isinstance(item['embedding_resposta'], list):                 item['embedding_resposta'] = np.array(item['embedding_resposta'], dtype=np.float64)             else:                 item['embedding_resposta'] = item['embedding_resposta'].astype(np.float64)             BASE_EMBEDDINGS[item['id']] = item # Armazena o item completo             MEMORY_EMBEDDINGS[item['id']] = item['embedding_pergunta'] # Armazena apenas o embedding para busca rápida     print(f"{len(BASE_EMBEDDINGS)} itens de conhecimento e embeddings carregados na memória.") def regenerar_embeddings_conhecimento_existente():     print("--- Regenerando embeddings da base de conhecimento ---")     global BASE_EMBEDDINGS, MEMORY_EMBEDDINGS     for categoria_info in categorias_dados:         categoria_nome = categoria_info['nome']         # Load knowledge for the category. This is where the error occurred before.         conhecimento_categoria = carregar_conhecimento(categoria_nome)         # If the knowledge for the category is empty (due to a corrupted file or non-existence),         # there's nothing to regenerate, so continue to the next category.         if not conhecimento_categoria:             print(f"  '{categoria_nome}': Nenhum item para regenerar (arquivo vazio ou corrompido).")             continue         itens_regenerados_count = 0         for item in conhecimento_categoria:             # Gerar novos embeddings para pergunta e resposta             # Ensure the text is decrypted before generating embedding             pergunta_desc = item['pergunta'] # Already decrypted by carregar_conhecimento             resposta_desc = item['resposta'] # Already decrypted by carregar_conhecimento             novo_embedding_pergunta = gerar_embedding(pergunta_desc)             novo_embedding_resposta = gerar_embedding(resposta_desc)             # Atualizar o item             # Convert to list for JSON serialization to avoid TypeError later             item['embedding_pergunta'] = novo_embedding_pergunta.tolist()             item['embedding_resposta'] = novo_embedding_resposta.tolist()             itens_regenerados_count += 1         # Salvar o conhecimento atualizado para a categoria         if itens_regenerados_count > 0:             salvar_conhecimento(conhecimento_categoria, categoria_nome)             print(f"  '{categoria_nome}': {itens_regenerados_count} itens regenerados.")     recarregar_base_de_conhecimento() # Recarregar tudo na memória após a regeneração     print("--- Regeneração concluída.---") def embedding_categoria_com_exemplos(descricao, exemplos, encoder):     textos = [descricao] + exemplos     embs = [encoder(t) for t in textos]     return np.mean(embs, axis=0) import numpy as np from sklearn.metrics.pairwise import cosine_similarity # ============================================================ # BLOCO 10 — GERAÇÃO DE EMBEDDINGS # ============================================================ def gerar_embedding(texto):     if isinstance(texto, list):         # Se o texto for uma lista de strings, gera embeddings para cada uma         embeddings = modelo.encode([preprocessar_texto(t) for t in texto], convert_to_numpy=True)     else:         # Se for uma única string, gera o embedding para ela         embeddings = modelo.encode(preprocessar_texto(texto), convert_to_numpy=True)     return embeddings.astype(np.float64) # Explicitamente converter para float64 # ============================================================ # BLOCO 9 — UTILITÁRIOS DE TEXTO # ============================================================ def remover_acentos(texto):     if isinstance(texto, str):         nfkd_form = unicodedata.normalize('NFKD', texto)         return ''.join([c for c in nfkd_form if not unicodedata.combining(c)])     return texto def preprocessar_texto(texto, remover_stopwords=True, lematizar=False, stemizar=False):     if not isinstance(texto, str):         return ""     texto = texto.lower()     texto = remover_acentos(texto)     texto = re.sub(r'[^a-z0-9\s]', '', texto) # Remove caracteres especiais, mantem letras e numeros     tokens = word_tokenize(texto, language='portuguese')     if remover_stopwords:         stop_words = set(stopwords.words('portuguese'))         tokens = [token for token in tokens if token not in stop_words]     if lematizar:         doc = nlp_pt(" ".join(tokens)) # Processa com SpaCy         tokens = [token.lemma_ for token in doc]     if stemizar:         stemmer = RSLPStemmer()         tokens = [stemmer.stem(token) for token in tokens]     return " ".join(tokens) def extrair_palavras_chave(pergunta):     doc = nlp_pt(pergunta.lower())     palavras_chave = []     for token in doc:         # Exclui stopwords, pontuações, espaços e termos que não são substantivos ou adjetivos         if not token.is_stop and not token.is_punct and not token.is_space and token.pos_ in ['NOUN', 'PROPN', 'ADJ']:             palavras_chave.append(token.text)     return ' '.join(palavras_chave) def calcular_similaridade_fuzzy(texto1, texto2):     return fuzz.ratio(texto1, texto2) / 100.0 def calcular_similaridade_cosseno(embedding1, embedding2):     # Ensure inputs are numpy arrays for cosine_similarity and float64 dtype     if not isinstance(embedding1, np.ndarray):         embedding1 = np.array(embedding1, dtype=np.float64)     if not isinstance(embedding2, np.ndarray):         embedding2 = np.array(embedding2, dtype=np.float64)     # Reshape if 1D array to 2D for sklearn's cosine_similarity     if embedding1.ndim == 1:         embedding1 = embedding1.reshape(1, -1)     if embedding2.ndim == 1:         embedding2 = embedding2.reshape(1, -1)     return cosine_similarity(embedding1, embedding2)[0][0] def entidade_match(pergunta_original, item_texto):     # Processa os textos com SpaCy para extrair entidades nomeadas     pergunta_doc = nlp_pt(pergunta_original.lower())     item_texto_doc = nlp_pt(item_texto.lower())     entidades_pergunta = {ent.text for ent in pergunta_doc.ents if ent.label_ in ['PERSON', 'ORG', 'LOC', 'MISC']}     entidades_item = {ent.text for ent in item_texto_doc.ents if ent.label_ in ['PERSON', 'ORG', 'LOC', 'MISC']}     # Retorna True se houver alguma entidade em comum     return bool(entidades_pergunta.intersection(entidades_item)) # 1. PRÉ-PROCESSAMENTO (do pseudocódigo) def normalizar(texto):     return texto.lower().strip() # 2. DETECÇÃO DE INTENÇÃO (CRÍTICO) (do pseudocódigo) # Assumes INTENCOES is a global variable from BLOCO 5 def detectar_intencao(pergunta):     pergunta_normalizada = normalizar(pergunta)     for tipo, padrões in globals()['INTENCOES'].items(): # Access global INTENCOES         for p in padrões:             if p in pergunta_normalizada:                 return tipo     return "geral" # 5. FILTRO ESTRUTURAL (do pseudocódigo) def mesma_intencao(p1, p2):     return detectar_intencao(p1) == detectar_intencao(p2) def overlap(p1, p2):     s1 = set(normalizar(p1).split())     s2 = set(normalizar(p2).split())     return len(s1 & s2) / max(len(s1), 1) # ============================================================ # BLOCO 7 — MODELO DE EMBEDDING (carregado uma vez) # ============================================================ modelo = SentenceTransformer('sentence-transformers/paraphrase-multilingual-mpnet-base-v2') print("Modelo de embedding carregado.") import numpy as np from sklearn.metrics.pairwise import cosine_similarity def detectar_categoria(pergunta, categorias, encoder):     """     categorias: dict {nome: descricao}     encoder: função que gera embedding     """     emb_pergunta = encoder(pergunta)     scores = []     for nome, descricao in categorias.items():         emb_cat = encoder(descricao)         sim = cosine_similarity(emb_pergunta.reshape(1, -1), emb_cat.reshape(1, -1))[0][0]         scores.append((nome, sim))     # Ordena do maior pro menor     scores.sort(key=lambda x: x[1], reverse=True)     top1_nome, top1_score = scores[0]     top2_score = scores[1][1] if len(scores) > 1 else 0     #  Confiança real     gap = top1_score - top2_score     confianca = (top1_score * 0.7) + (gap * 0.3)     return {         "categoria": top1_nome,         "score": round(top1_score, 3),         "gap": round(gap, 3),         "confianca": round(confianca, 3),         "top_k": scores[:3]     } def decidir_categoria(resultado):     score = resultado["score"]     confianca = resultado["confianca"]     if score >= 0.75 and confianca >= 0.65:         return "alta_confianca"     elif score >= 0.55 and confianca >= 0.45:         return "media_confianca"     else:         return "baixa_confianca" # ============================================================ # BLOCO 5 — CONFIGURAÇÕES GLOBAIS # ============================================================ # Definição das categorias e suas descrições robustas para melhor categorização semântica categorias_dados = [     {         "nome": "saude",         "descricao": "Saúde humana e animal, medicina, diagnóstico de doenças, sintomas, tratamentos, gripe, febre, dor, hospitais, médicos, fisiologia, anatomia, bem-estar físico, prevenção e intervenções clínicas.",         "exemplos": ["Como tratar uma gripe?", "Sintomas de febre", "O que um médico faz?"]     },     {         "nome": "ciencia",         "descricao": "Ciência natural, física, química, biologia, astronomia, geologia, método científico, leis da natureza, átomos, moléculas, células, evolução, gravidade, energia, matéria e experimentos científicos.",         "exemplos": ["O que é um átomo?", "Como funciona a gravidade?", "Leis da termodinâmica"]     },     {         "nome": "matematica",         "descricao": "Matemática pura e aplicada, números, aritmética, álgebra, geometria, cálculo, estatística, lógica matemática, equações, fórmulas, teoremas, contagem e raciocínio lógico formal.",         "exemplos": ["Quanto é 2 mais 2?", "O que é uma equação?", "Fórmula de Bhaskara"]     },     {         "nome": "tecnologia",         "descricao": "Tecnologia da informação, computação, software, hardware, inteligência artificial, programação, redes, internet, dispositivos eletrônicos, inovação digital e sistemas de engenharia.",         "exemplos": ["O que é IA?", "Como funciona a internet?", "Linguagens de programação"]     },     {         "nome": "filosofia",         "descricao": "Filosofia, ética, moral, existencialismo, lógica formal, história do pensamento, grandes filósofos, metafísica, epistemologia e reflexões sobre a vida e a sociedade.",         "exemplos": ["O que é a verdade?", "Quem foi Sócrates?", "Ética e moral"]     },     {         "nome": "entretenimento",         "descricao": "Cultura pop, filmes, séries, música, jogos, celebridades, artes, literatura de ficção, esportes e lazer em geral.",         "exemplos": ["Melhores filmes de 2024", "Quem ganhou o Oscar?", "Regras do futebol"]     },     {         "nome": "geral",         "descricao": "Assuntos gerais, curiosidades, informações do dia a dia, conhecimentos variados que não se encaixam em categorias específicas.",         "exemplos": ["Qual a capital da França?", "Como fazer um café?", "Curiosidades sobre o mundo"]     } ] BASE_PATH              = "/content/drive/MyDrive/RAVENA/RAVENA_MODULAR" CONHECIMENTO_BASE_PATH = os.path.join(BASE_PATH, "data") CONVERSA_PATH          = os.path.join(BASE_PATH, "data", "conversa.json") CHAVE_PATH             = os.path.join(BASE_PATH, "data", "chave.key") # ── Thresholds gerais ────────────────────────────────────── GLOBAL_MATCH_THRESHOLD              = 0.65 # Ajustado conforme o novo fluxo SEMANTIC_THRESHOLD                  = 0.40 # Ajustado conforme o novo fluxo FUZZY_THRESHOLD                     = 0.6 MAX_MEMORIA_POR_TOPICO              = 100 MAX_VARIACOES_POR_CONCEITO          = 10 CATEGORIA_MIN_SCORE                 = 0.30 # Novo, para detecção multi-categoria # ── Thresholds de busca e aprendizado ───────────────────── UPDATE_EXISTING_KNOWLEDGE_THRESHOLD = 0.85  # sim. mínima para atualizar item existente LOW_CONFIDENCE                      = 0.45  # score mínimo para resposta de fallback HIGH_CONFIDENCE                     = 0.70  # score mínimo para resposta completa MIN_ITENS_CLUSTER                   = 5     # mínimo de itens para clusterizar # ── Auto-aprendizagem ────────────────────────────────────── LIMITE_CONFIANCA_MIN_AUTO_APRENDER  = 0.8 LIMITE_CONFIANCA_MAX_AUTO_APRENDER  = 3.0 MIN_COMPRIMENTO_RESPOSTA_AUTO_APRENDER = 15 PONTUACAO_MIN_AUTO_APRENDER         = 5.0 PONTUACAO_MAX_AUTO_APRENDER         = 10.0 # ── Pesos dos itens ──────────────────────────────────────── PESO_MAX = 5.0 PESO_MIN = 0.5 PESOS = {     "fuzzy":                            0.1,     "semantic":                         0.9,     "semantic_question_weight":         0.7,     "semantic_response_weight":         0.3,     "confidence_bonus_per_unit":        0.01,     "access_bonus_per_access":          0.001,     "category_match_bonus":             0.1,     "intention_match_bonus_multiplier": 1.0,     "short_term_memory_bonus":          0.1,     "decay_rate_confidence":            0.02,     "decay_rate_weight":                0.01, } INTENTION_BONUS = {     'definicao':       0.025,     'causa':           0.015,     'efeito':          0.015,     'historico':       0.025,     'comparacao':      0.02,     'geral':           0.005,     'saudacao':        0.01,     'saudacao_formal': 0.015, } # INTENCOES para o pipeline (novo) INTENCOES = {     "definicao": ["o que é", "o que são", "explique"],     "procedimento": ["como", "como fazer"],     "causa": ["por que", "o que causa"],     "comparacao": ["diferença", "compare"],     "identificacao": ["qual", "quem"],     "risco": ["perigo", "riscos"] } # ── Peso de rigor por categoria ──────────────────────────── PESOS_DORMANCIA_CATEGORIA = {     'ciencia': 1.0, 'tecnologia': 1.0, 'espaco': 1.0, 'matematica': 1.0,     'saude': 0.95,  'logistica': 0.95,     'politica': 0.9, 'economia': 0.9, 'historia': 0.9,     'geografia': 0.85, 'filosofia': 0.85,     'arte': 0.8, 'religiao': 0.8,     'esporte': 0.75, 'linguagem': 0.75, 'linguistica': 0.75,     'entretenimento': 0.7, 'culinaria': 0.7, 'geral': 0.7, } MODO_EXPLICACAO      = "tecnico"   # "tecnico" | "simples" | "oculto" LIMITE_MEMORIA_CURTA = 5 memoria_curta        = deque(maxlen=LIMITE_MEMORIA_CURTA) # ============================================================ # BLOCO 4 — DADOS NLTK # ============================================================ for _res in ['corpora/stopwords', 'tokenizers/punkt', 'tokenizers/punkt_tab']:     try:         nltk.data.find(_res)     except LookupError:         nltk.download(_res.split('/')[-1]) print("NLTK: recursos carregados.") # ============================================================ # BLOCO 3 — CARREGAMENTO DO SPACY # ============================================================ try:     nlp_pt = spacy.load('pt_core_news_sm')     print("SpaCy 'pt_core_news_sm' carregado.") except OSError:     os.system("python -m spacy download pt_core_news_sm")     nlp_pt = spacy.load('pt_core_news_sm') except Exception as e:     print(f"Erro SpaCy: {e}. Usando fallback 'en_core_web_sm'...")     try:         nlp_pt = spacy.load('en_core_web_sm')     except OSError:         os.system("python -m spacy download en_core_web_sm")         nlp_pt = spacy.load('en_core_web_sm')     print("SpaCy fallback carregado.") # ============================================================ # BLOCO 2 — IMPORTS # ============================================================ import os, json, re, unicodedata, shutil, time, uuid, ast import numpy as np import spacy import nltk from sentence_transformers import SentenceTransformer from sklearn.metrics.pairwise import cosine_similarity from sklearn.cluster import KMeans from cryptography.fernet import Fernet from collections import deque from nltk.corpus import stopwords from nltk.stem import RSLPStemmer from nltk.tokenize import word_tokenize try:     from fuzzywuzzy import fuzz except ImportError:     import subprocess     subprocess.run(["pip", "install", "fuzzywuzzy", "python-Levenshtein"])     from fuzzywuzzy import fuzz # ============================================================ # BLOCO 1 — INSTALAÇÕES (execute uma vez no Colab) # ============================================================ # !pip install sentence-transformers scikit-learn cryptography # !pip install fuzzywuzzy python-Levenshtein nltk spacy # !python -m spacy download pt_core_news_sm